```py
class Solution:
    def removeElement(self, nums: List[int], val: int) -> int:
        slow = 0
        fast = 0
        while fast < len(nums):
            if nums[fast] != val:
                nums[slow], nums[fast] = nums[fast], nums[slow]
                slow += 1 #只有跟val相同的數值才需要紀錄 並加一格新的位置給它
            fast += 1
        return slow #回傳有放數值的位置
```

        
          
